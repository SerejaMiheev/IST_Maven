package gw;
/*
import exceptions.EntityNotFound;
import models.Equipment;
import registry.GWRegistry;

public class EquipmentGateway extends SimpleGateway<Equipment> {

    public EquipmentGateway() {
        try {
            TypeGateway typeGateway = new TypeGateway();
            this.insert(new Equipment(typeGateway.find(1), 10));
            this.insert(new Equipment(typeGateway.find(3), 0));
            this.insert(new Equipment(typeGateway.find(2), 150));
        } catch (EntityNotFound e) {
        }
    }
}
*/